function soma(a, b){
    return a + b;
}

soma(2, 2);  
soma(2, 5);
soma(2, 2);
soma(2, 8);

// vendo que o primeiro parametro usado da funçao soma acima
// é repetido durante os outros a modificação do segundo parametro
// COM ISSO PODEMOS USAR O CURRYING  que seria definir um dos parametros
// e só modificar o segundo. Veja isso no arquivo currying2.js dentro da pasta conceitos
